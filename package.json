{
  "name": "pets",
  "version": "1.0.0",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "Root package.json for the monorepo",
  "private": true,
  "scripts": {
    "build:all": "pnpm --parallel build", 
    "test:all": "pnpm --parallel test" 

  },
  "devDependencies": {
    "typescript": "^5.0.0", 
    "@nestjs/cli": "^10.0.0"
  },
  "resolutions": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/typeorm": "^10.0.0",
    "typeorm": "^0.3.0"
  }
}
